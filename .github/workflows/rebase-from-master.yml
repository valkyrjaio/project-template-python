name: Rebase from Master

on:
  workflow_dispatch:

run-name: "Rebase ${{ github.ref_name }} from master"

jobs:
  rebase:
    uses: valkyrjaio/.github/.github/workflows/_rebase-from-master.yml@1243d6eb0a7b9425f22295e0097dcf70e8276322
    permissions:
      contents: write
    secrets: inherit
